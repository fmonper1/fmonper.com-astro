<div class="animationWrap" style="height: 24px; width: 100%;">
  <div class="el"></div>
  <div class="el square"></div>
  <div class="el triangle"></div>
</div>

<style>
  .el {
    position: absolute;
    animation-name: go;
    animation-iteration-count: infinite;
    width: 12px;
    height: 12px;
    animation-duration: 10s;
    border-radius: 100px;
    @apply bg-primary;
  }

  .square {
    border-radius: 0;
    animation-delay: 2s;
  }

  .triangle {
    border-radius: 0;
    width: 0;
    height: 0;

    border-left: 6px solid transparent;
    border-right: 6px solid transparent;

    border-bottom: 12px solid currentColor;
    animation-delay: 4s;
    background: transparent !important;

    @apply text-primary;
  }

  @keyframes go {
    25% {
      transform: translate(0px, 24px);
      @apply bg-accent text-accent;
    }

    50% {
      transform: translate(300px, 24px);
      @apply bg-accent;
    }

    75% {
      transform: translate(300px, 0px);
      @apply bg-primary;
    }
  }
</style>
